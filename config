# Hostname for the repository
docker_host="docker.io"

# Namespace of the image, if any
namespace="joshhogle"

# Name of the image to upload to the docker registry
imgname="plexmediaserver"

# Files that need to be copied for the build
src_dirs="plexmediaserver/files"

# Additional tags to apply to the generated image
extra_tags="latest"

# List of Dockerfiles required to generate the image
dockerfiles="plexmediaserver/Dockerfile"

# Base image to pull from
image_from="docker.io/joshhogle/ecloudfs:latest"

